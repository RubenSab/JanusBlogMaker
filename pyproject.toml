[build-system]
requires = ["setuptools>=61.0", "plac"]
build-backend = "setuptools.build_meta"

[project]
name = "janus-blog-maker"
version = "0.1.0"
dependencies=["plac"]
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
janusBM = "main:cli"